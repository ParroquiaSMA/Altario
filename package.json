{
  "name": "altario",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "npm run dev:web",
    "dev:web": "(lsof -ti:4321 | xargs kill -9 2>/dev/null || true) && npm --prefix web run dev",
    "dev:cms": "(lsof -ti:4322 | xargs kill -9 2>/dev/null || true) && npm --prefix cms run dev",
    "build": "npm --prefix web run build && npm --prefix cms run build",
    "build:web": "npm --prefix web run build",
    "build:cms": "npm --prefix cms run build",
    "check": "npm --prefix web run check && npm --prefix cms run check",
    "typecheck": "npm --prefix web run typecheck && npm --prefix cms run typecheck",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.3",
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.2.4",
    "tailwindcss": "^4.3.3"
  }
}
